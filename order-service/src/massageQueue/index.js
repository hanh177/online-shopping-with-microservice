const EXCHANGES = {
  ORDER: "order_exchange",
};
const EVENTS = {
  ORDER_CREATED: "order_created",
  ORDER_CANCELED: "order_canceled",
};

const QUEUES = {
  PRODUCT_ORDER_QUEUE: "product_order_queue",
};

module.exports = { EXCHANGES, EVENTS, QUEUES };
